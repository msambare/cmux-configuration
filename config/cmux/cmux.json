{
  "$schema": "https://raw.githubusercontent.com/manaflow-ai/cmux/main/web/data/cmux.schema.json",
  "schemaVersion": 1,

  // cmux-configuration — managed cmux app settings (cmux-only; no personal data; safe to share).

  "app": {
    "appearance": "dark"
  },

  // Sidebar tuned to harmonize with the shared Catppuccin Mocha terminal theme.
  "sidebarAppearance": {
    "matchTerminalBackground": false,
    "tintColor": "#181825",
    "tintOpacity": 0.85
  },

  "workspaceColors": {
    "indicatorStyle": "leftRail",
    "selectionColor": "#313244",
    "notificationBadgeColor": "#f38ba8"
  },

  // Hybrid keybindings: tmux Ctrl+B prefix for pane/split/window nav; cmux Cmd defaults
  // stay for app-level (palette cmd+shift+p, settings cmd+,, sidebar cmd+b, new workspace
  // cmd+n, workspace switcher cmd+p, find, browser, quit). See docs/keybindings.md.
  // Note: %/" express as shift+5 / shift+' (cmux key grammar has no literal %/").
  "shortcuts": {
    "bindings": {
      "splitRight":            ["ctrl+b", "shift+5"],
      "splitDown":             ["ctrl+b", "shift+'"],
      "focusLeft":             ["ctrl+b", "left"],
      "focusRight":            ["ctrl+b", "right"],
      "focusUp":               ["ctrl+b", "up"],
      "focusDown":             ["ctrl+b", "down"],
      "toggleSplitZoom":       ["ctrl+b", "z"],
      "equalizeSplits":        ["ctrl+b", "space"],
      "newSurface":            ["ctrl+b", "c"],
      "nextSurface":           ["ctrl+b", "n"],
      "prevSurface":           ["ctrl+b", "p"],
      "selectSurfaceByNumber": ["ctrl+b", "1"],
      "closeTab":              ["ctrl+b", "x"],
      "renameTab":             ["ctrl+b", ","],
      "toggleTerminalCopyMode":["ctrl+b", "["]
    }
  }
}
